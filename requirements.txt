mujoco>=3.2.7
numpy>=1.24,<2.0
matplotlib>=3.7
imageio>=2.31
pytest>=8.0
# torch is required for generate_motions.py — install with CUDA support:
# pip install torch --index-url https://download.pytorch.org/whl/cu121
torch
